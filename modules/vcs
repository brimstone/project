# TODO Check for vcs

register vcs vcs_check

vcs_check() {
	check "Checking for VCS"
	if git branch >/dev/null 2>/dev/null; then
		pass
	else
		fail
		return -1
	fi
# TODO Check for remote vcs
}

# vim: filetype=sh
